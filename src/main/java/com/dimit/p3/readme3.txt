在第二部分中可以看到：
使用访问者模式后每增加一个操作（Visitor）时只需要使新加的visitor中实现AbstractVisitor抽象类中的想要实现的方法即可。
可见这种设计满足OCP原则。但存在的问题就是当增加一个元数据时（Person的子类）我们必须去修改Visitor接口以满足要求。从而导致
元数据对visitor的依赖过于紧密。在接下来这个部分将着重处理对这部分依赖的解耦。
